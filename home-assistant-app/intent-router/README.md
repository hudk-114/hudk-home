# HUDK Home Intent Router App

Home Assistant App manifest for the Node.js Intent Router in this repository.

Runtime images are published as `ghcr.io/hudk-114/hudk-home-intent-router:<version>` for `amd64` and `aarch64`. The version here must match `intent-router/package.json` before release.
